{
  "name": "ai-dev-assistant",
  "version": "1.0.0",
  "private": true,
  "description": "Professional AI developer assistant built with React, Node.js, Express and OpenAI API.",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client",
    "start": "npm start --prefix server"
  },
  "keywords": ["ai", "react", "nodejs", "express", "openai", "portfolio"],
  "author": "Zouheir Jaida",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
